P Simple {
  S Container {
    nat x
    |
    x <- 1
  };
  S Simple {
    bool g;
    Container c1, c2, tmp, c
    |
    tmp.x <- c1.x + c2.x
  }
  if g then
    c.x <- (tmp + c1.x) * c2.x
  else
    c.x <- tmp + (c1.x / c2.x)
  fi;
  Q task {on Simple in c1, c2, g out c}
}